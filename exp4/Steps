Remember it is only 4th qn for 5th qn refer exp5 folder

do these after puling in local machine


sudo docker build -t app:1.0 .

sudo usermod -aG docker $USER
newgrp docker

minikube start --driver=docker

kubectl apply -f app.yaml

 kubectl get all -n exp4

 minikube service app -n exp4 --url

